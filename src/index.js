import init from 'TOUCH/index.js';

const imageCheck = {
    init () {
        init();
    },
    bind (dom) {
        bind(dom);
    }
};

imageCheck.init();
imageCheck.bind('.img');

// module.exports = imageCheck;
